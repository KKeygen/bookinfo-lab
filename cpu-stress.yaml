apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-productpage
  namespace: bookinfo
spec:
  mode: one
  selector:
    namespaces: [bookinfo]
    labelSelectors:
      app: productpage
  stressors:
    cpu:
      workers: 4
      load: 100
  duration: "2m"
